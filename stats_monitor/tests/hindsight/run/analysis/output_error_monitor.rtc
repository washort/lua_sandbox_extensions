-- original configuration
filename = "output_error_monitor.lua"
message_matcher = 'Type == "hindsight.plugins"'
process_message_inject_limit = 2
alert = {
  disabled = false,
  prefix = false,
  throttle = 1440, -- default to once a day (inactivity and ingestion_error default to 90)
  modules = {
    email = {recipients = {"trink@mozilla.com"}},
  },

  thresholds = {
    ["output.test_percent"] = {
      percent = 5
    },
    ["output.test_count"] = {
      number = 5
    }
  }
}

-- Hindsight defaults and overrides
Hostname = [[integration_test]]
Pid = 11584
log_level = 7
path = [[/home/washort/Projects/nix-hindsight/result/share/luasandbox/modules/?.lua;/home/washort/Projects/nix-hindsight/result/lib/luasandbox/modules/?.lua]]
cpath = [[/home/washort/Projects/nix-hindsight/result/lib/luasandbox/modules/?.so]]

-- Sandbox defaults and overrides
Logger = [[analysis.output_error_monitor]]
output_limit = 0
memory_limit = 0
instruction_limit = 0
ticker_interval = 0
preserve_data = false
restricted_headers = true
shutdown_on_terminate = false
thread = 0
process_message_inject_limit = 2
timer_event_inject_limit = 10
-- end Hindsight configuration
